# 200000100 맵의 back 표시 조사

## 확인 조건

- 데이터: `C:\Nexon\Maple\Data\Base\Base.wz`, `Map\Map\Map2\200000100.img`
- 기존 DLL: `D:\MapleStory_UnrealSource\Game\Bin\WzNativeLib.dll`
- 현재 main 카메라: `(100,0)`, 줌 1, 뷰포트 1366x768, DisplayMode 2
- 배경 위치 계산 시각: 맵 시작 시점 0ms
- 빌드와 게임 실행 없이 DLL 반환 데이터와 원본 픽셀 알파를 읽었다.

## 확인된 사실

배치 23개 모두 이미지 로딩에 성공했다. 각 항목은 일반 정적 이미지 1프레임이며,
screenMode=0, front=0, 아이템/프레임 알파=255, flow 없음이다.
어떤 이미지도 완전히 투명하지 않다. 이 맵에서 Spine 미지원이나 해상도 필터가
배경을 누락시키지는 않는다.

`skyStation/back/13`은 맵 back 슬롯 13이고 type=4, 크기 400x69이다.
시작 시점의 첫 이미지 화면 영역은 `(165,498)~(565,567)`이고, 가로로 반복된다.

## 13번 이미지 집중 조사

사용자가 확인한 누락 의심 대상은 **13번 이미지**다.
맵용 `wz_anim_load_back`이 반환한 실제 픽셀은 `back13-source.png`로 저장했다.
원본 이미지의 origin은 `(200,34)`이며, 아이템/프레임 알파는 모두 255다.

맵의 타일과 오브젝트도 같은 기존 DLL로 읽어 시작 시점의 픽셀 알파를 대조했다.
이미지 추출 실패는 없었다. 다음 수치는 화면에서 back 13의 기여가 남을 수 있는
픽셀 수이며, 실제 GPU 렌더링 결과를 측정한 수치는 아니다.

| 가림을 반영한 단계 | 13번의 남은 영역 픽셀 |
| --- | --- |
| 구름 자체의 화면 안 알파 영역 | 77001 |
| 레이어 4 오브젝트 반영 후 | 58860 |
| 레이어 5 타일 반영 후 | 1842 |
| 레이어 6 오브젝트 반영 후 | 1795 |
| 더 나중에 그리는 back 14~22까지 반영 후 | 1680 |

특히 레이어 4 오브젝트는 화면 Y=469~526, 레이어 5 타일은 Y=523~564 및
564~624에 놓여 구름의 Y=498~567 영역을 덮는다. 약 2.2%만 남으므로
13번 구름 띠가 출력되지 않은 것처럼 보일 수 있다.

현재 데이터는 front=0이므로 이 배경을 타일·오브젝트보다 먼저 그리는 것이
MapRender2 컨테이너 규칙과 같다. 확인되지 않은 누락을 가정해서 그리기 순서를
바꾸지는 않았다. 비교 기준은 프레임 0과 시작 시각이며, 포털·리액터·캐릭터는
가림 계산에 포함하지 않았다. 자세한 결과는 `back13-occlusion-data.json`에 있다.

## 나머지 배경의 화면 범위

현재 카메라에서 슬롯 17/18은 화면 오른쪽 밖이다. 슬롯 7은 다른 back들에
대부분 가려진다. 이는 DLL 누락과 구분해야 한다.
맵 VR 범위는 `(-895,-403)~(1523,370)`이며, 카메라는 현재 매 프레임 `(100,0)`으로
고정되어 있어 맵 전체를 동시에 보여주지 않는다.

## 항목별 결과

화면 영역은 반복 오프셋 0의 원본 이미지 사각형이다. 화면 밖의 원본이라도 type에
따라 이웃 반복 이미지가 보일 수 있다. 아래 가림 계산은 다른 back의 알파만
반영하며 타일·오브젝트·캐릭터·GPU 상태는 포함하지 않는다.

| 맵 back 슬롯 | skyStation/back 이미지 번호 | type | 화면에 걸친 이미지 수 | 뒤의 back에 가려지고 남은 알파 영역 픽셀 | 결과 |
| --- | --- | --- | --- | --- | --- |
| 0 | 0 | 3 | 448 | 252284 | 화면 안 |
| 1 | 1 | 1 | 138 | 240403 | 화면 안 |
| 2 | 2 | 4 | 2 | 8628 | 화면 안 |
| 3 | 3 | 4 | 1 | 12944 | 화면 안 |
| 4 | 4 | 0 | 1 | 75598 | 화면 안 |
| 5 | 5 | 0 | 1 | 31243 | 화면 안 |
| 6 | 6 | 0 | 1 | 6846 | 화면 안 |
| 7 | 7 | 0 | 1 | 378 | 대부분 다른 back에 가려짐 |
| 8 | 8 | 0 | 1 | 3274 | 화면 안 |
| 9 | 9 | 4 | 5 | 50174 | 화면 안 |
| 10 | 10 | 1 | 1 | 12768 | 화면 안 |
| 11 | 11 | 1 | 1 | 9781 | 화면 안 |
| 12 | 12 | 0 | 1 | 35465 | 화면 안 |
| 13 | 13 | 4 | 5 | 71791 | 화면 안 |
| 14 | 21 | 0 | 1 | 46136 | 화면 안 |
| 15 | 20 | 0 | 1 | 65516 | 화면 안, 왼쪽 일부 잘림 |
| 16 | 31 | 0 | 1 | 25155 | 화면 안 |
| 17 | 19 | 0 | 0 | 0 | 화면 x=1890~2324, 오른쪽 밖 |
| 18 | 31 | 0 | 0 | 0 | 화면 x=1745~1980, 오른쪽 밖 |
| 19 | 22 | 1 | 5 | 34931 | 화면 안 |
| 20 | 23 | 1 | 5 | 84922 | 화면 안, 위쪽 일부 잘림 |
| 21 | 24 | 1 | 4 | 38804 | 화면 안, 위쪽 일부 잘림 |
| 22 | 25 | 1 | 4 | 38336 | 화면 안, 위쪽 일부 잘림 |

## C++ 실행에서 남은 확인

DLL의 정상 반환만으로 실제 C++ GPU 업로드 및 화면 출력을 확인할 수는 없다.
FMapLoader/FMapScene에 `[MapBack]` 진단 로그를 추가했다.

- `loaded` / `load failed`: 항목별 C++ 로딩 결과
- `load summary`: 정상 로딩 수와 전체 배치 수
- `filtered by screenMode`: 해상도 조건으로 제외한 항목
- `skipped without texture`: 렌더링 프레임에 텍스처가 없는 항목
- `submitted`: 항목별 최초 제출 위치, 반복 범위와 간격, 알파

렌더링 로그는 항목당 최초 한 번만 출력한다. 로그를 추가하면서 배치·정렬·가시성
규칙은 변경하지 않았다. 실행 파일의 logs 폴더에서 `[MapBack]`을 확인할 수 있다.
13번의 완전한 미출력을 의심할 경우 이 실행 로그로 C++ 로딩 성공과 제출 여부를
확인한다. 이미지가 제출되어도 앞쪽 맵에 가려진 영역은 화면에 드러나지 않는다.

원본 측정 데이터는 `back-visibility-data.json`에 저장했다.
커밋 문구: `chore: 맵 배경 로딩과 렌더링 제출 진단 로그 추가`

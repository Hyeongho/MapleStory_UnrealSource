# MapRender2 프레임 맵 렌더링 연동

## 수정 위치와 적용

- C++: `D:\MapleStory_UnrealSource_Codex`, `codex/maprender2-parity`
- DLL 소스: `D:\WzComparerR2_cpp\WzComparerR2`, `codex/maprender2-metadata`

두 저장소의 소스를 함께 반영한다. DLL을 다시 빌드하여 게임 실행 파일이 로드하는
`WzNativeLib.dll`을 교체해야 새 해석 규칙이 적용된다. 이번 작업에서는 빌드,
테스트 실행, 바이너리 교체를 하지 않았다. 공개 export의 함수 인자와 Pack=4 구조체
레이아웃은 유지했다. Engine을 빌드하면 기존 Copy.bat이 GameEngine 공개 헤더를 갱신한다.

## DLL의 책임

`MapAnimationLoader`는 UOL과 이미지 링크를 해석하고 BGRA 픽셀, origin, delay,
프레임 z, a0/a1, blend, 전체 프레임 경계와 flow 정보를 반환한다.
`repeat`가 없으면 1, 명시적인 0이면 0이다. 단일 프레임도 같은 규칙을 적용한다.
픽셀 blob과 프레임 메타데이터 배열은 별도 할당이며 C++에서 각각 wz_free로 해제한다.

`wz_map_read_info`는 요청한 맵의 기본 정보를 반환한다. 다른 배치 API는
`info/link`가 있으면 연결된 맵의 배경·레이어·발판·포털·리액터를 반환한다.
MapData.Load처럼 링크는 한 번만 해석한다. 대상이 없거나 IMG 추출에 실패하면
빈 결과를 반환한다. C++에서 링크를 중복 해석할 필요는 없다.

DLL 저장소의 기존 wz_test.cpp도 wz_read_canvas의 실제 8개 인자 선언에 맞췄다.

## C++의 프레임 재생과 배경

배경·타일은 FMapScene, 오브젝트·포털·리액터는 액터와 스프라이트/플립북이 그린다.
모두 DLL의 repeat를 사용한다. 비반복 애니메이션은 마지막 프레임과 A1 알파에서
정지한다. 단일 프레임이라도 A0와 A1이 다르면 시간에 따라 알파를 보간한다.

배경 위치는 MapRender2 GetMeshBack의 순서를 따른다.

1. WZ type 0~7을 반복·스크롤 플래그로 변환한다.
2. 어느 축이든 반복하고 cx/cy가 0이면 전체 프레임 경계 크기로 보충한다.
3. flow 정보가 있으면 원본과 같은 flow 분기를 사용한다. 일반 프레임도 대상이다.
4. 그 외에는 X/Y마다 스크롤 또는 카메라 시차를 각각 적용한다.
5. 최종 위치를 내림하고, 이 위치와 카메라 영역으로 반복 범위를 계산한다.
6. RenderQueue에서는 배경 시차를 다시 더하지 않는다.

스크롤 시간은 프레임 순환 시간과 별개로 누적된다. 카메라의 DisplayMode는
screenMode 배경 선택에 사용하며 뷰포트 크기와 별도로 지정한다.
현재 1366x768 데모는 DisplayMode=2이다. origin과 좌우 반전 보정은 유지한다.

### 구름 스크롤의 연속성 수정

로컬 `C:\Nexon\Maple\Data\Base\Base.wz`의 200000100 맵을 현재 main 게임 폴더의
기존 DLL로 읽어 확인한 배경 데이터는 다음과 같다. 빌드와 게임 실행은 하지 않았다.

| back 번호 | 리소스 | ani | type | rx | cx | 이동 속도 |
| --- | --- | --- | --- | --- | --- | --- |
| 2 | skyStation/back/2 | 0 | 4 | 2 | 900 | 10픽셀/초 |
| 3 | skyStation/back/3 | 0 | 4 | 3 | 1500 | 15픽셀/초 |
| 9 | skyStation/back/9 | 0 | 4 | 3 | 0 → 경계 너비 400 | 15픽셀/초 |

이 항목들은 flow 없는 정적 이미지이며, type=4의 가로 스크롤로 이동한다.
참고한 MapRender2의 공식은 W=0일 때 이동 오프셋을 100으로 나눈 나머지로 만든다.
그래서 반복 간격이 900이어도 100픽셀 이동 후 0으로 돌아온다. 이번에는 속도 계산의
기준 거리는 그대로 두고, 반복하는 축의 되감기 간격만 cx/cy로 분리했다.
예를 들어 back 2는 10초에 100픽셀 이동하고 90초에 900픽셀 주기를 끝낸다.
이때 이웃 반복 이미지가 동일 위치를 이어받으므로 화면의 무늬가 연속된다.

이 되감기 간격은 MapRender2 원본 공식과 의도적으로 다르다. 나머지 배치·시차·
프레임 규칙은 유지한다. type=0이고 flow도 없는 이미지는 시간에 따라 이동하지 않는다.
실제 구름 화면이 연속되는지는 사용자 실행 확인이 필요하다.

이 수정의 커밋 문구: `fix: 배경 스크롤 되감기 주기를 타일 반복 간격에 맞춤`

## 캐릭터와 오브젝트의 앞뒤 관계

정렬 키는 `(Layer, ContainerOrder, Z0, Z1)`이다.

```text
후면 배경
→ 레이어 0~7 각각: 오브젝트 → 리액터 → 타일 → 발판별 Life
→ 포털 → 발판에 속하지 않는 Sky
→ 전면 배경 → 효과 → UI
```

오브젝트는 배치 z, 타일·포털·리액터는 현재 프레임 z를 사용한다.
Z1은 WZ 노드 번호이고, Life의 ContainerOrder는 발판 생성 순서다.
서로 다른 컨테이너의 Z값을 섞어 정렬하지 않는다.

캐릭터는 `SetCharacterFoothold`로 발판 ID에 해당하는 레이어와 컨테이너를 선택한다.
같은 레이어의 오브젝트·타일은 캐릭터보다 먼저 그려지고, 더 높은 레이어의
오브젝트는 나중에 그려진다. 발판을 지정하지 않은 캐릭터는 Sky에 들어간다.

데모는 물리 연동 전이므로 시작 위치 아래의 가장 가까운 발판으로 렌더링 레이어만
설정한다. 이는 실제 접지 판정이 아니다. URigidbody가 있다면 월드 물리 갱신 이후
`UpdateCharacterLayer`가 실제 CurrentFootholdId를 사용한다. 점프 중 ID가 없으면
마지막 레이어를 유지한다. 이 유지 정책은 플레이어용 정책이며, MapRender2의
발판에 속하지 않는 Life를 Sky에 넣는 규칙과 구분된다.

맵 씬은 `UWorld`가 필요할 때만 생성하고 소유한다. 맵 재로드는 소유 월드에 연결된 씬의 `Clear()`로 이전 맵 액터와 씬 텍스처를 정리한다. `UWorld::DestroyMapScene()`은 씬을 제거하면서 캐릭터 등 외부 액터를 유지한다.
캐릭터와 외부 액터는 제거하지 않는다. 같은 씬은 로드에 사용한 월드와 함께 재사용한다.

## 범위와 확인 상태

기본 프레임 이미지의 배치·정렬·배경 이동·반복 규칙을 원본 소스와 대조했다.
Spine, NPC/몬스터 로딩, 포털/리액터 상태 전환, 오브젝트 이동 경로와 조건부 표시,
파티클 및 특수 셰이더는 이번 변경에 포함하지 않았다. Map.wz 발판을 PhysicsWorld에
등록하는 작업도 별도다. 실제 WZ 화면 일치와 컴파일 성공은 실행 검증 전이다.

사용자 확인 항목: repeat 미지정/0/1, 단일 프레임 알파, X/Y 시차가 다른 배경,
cx/cy=0인 한 축 반복, info/link 맵, 캐릭터보다 앞/뒤인 맵 레이어, 맵 재로드.

## 커밋 문구

DLL 저장소:

```text
fix: MapRender2 기준 WZ 애니메이션 반복과 연결 맵 해석 수정

- repeat 미지정 기본값을 반복으로 처리
- 요청 맵 정보와 info/link 대상 배치 데이터 분리
- wz_read_canvas 테스트 클라이언트의 8개 인자 ABI 수정
```

C++ 저장소:

```text
fix: MapRender2 기준 맵 애니메이션과 배경 및 캐릭터 정렬 연동

- DLL repeat와 프레임 알파·Z를 렌더링에 반영
- 배경 축별 시차와 스크롤 및 반복 범위 계산 수정
- 발판 레이어를 기준으로 캐릭터와 맵 오브젝트의 앞뒤 관계 적용
- 맵 재로드 액터 정리와 픽셀 버퍼 범위 검사 추가
```
